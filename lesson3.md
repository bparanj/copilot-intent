```javascript
/\*\*

- @param {string} s
  \*/
  // Write a function to getCharacterCount that takes a string as an argument and
  // returns the number of characters in that string.
  function getCharacterCount(s) {
  return s.length;
  }
  // log to console the result of calling your "getCharacterCount" function with the argument "hello world"
  console.log(getCharacterCount("hello world"));
```
