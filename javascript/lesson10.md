```javascript
// Create a JS doc comment that documents two string input parameters called first and last
/**
 * @param {string} first
 * @param {string} last
 */
// Write a function that concatenates the two input parameters and returns the result.
function concat(first, last) {
  return first + last;
}
// Print the result to the console.
console.log(concat("Hello ", "World"));
```
