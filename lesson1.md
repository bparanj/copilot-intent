```javascript
// Write a javascript function with name sum that takes two numbers as input and returns
// the sum of those numbers.
// Call the function with the numbers 13 and 124, and log the result to the console.
function sum(a, b) {
  return a + b;
}
console.log(sum(1, 2));
```
